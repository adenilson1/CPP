#include <cstdio>


int main() {

    // type identifier = value
    double price = 99.80;
    double tax = 0.08;

    // type identifier = expression
    double finalPrince = price * (1 + tax);

    printf("The final prince is: $ %.2f. \n", finalPrince);
    printf("Age = %d old", 35);

    return 0;
}